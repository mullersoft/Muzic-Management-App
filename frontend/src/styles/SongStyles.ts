import { css } from '@emotion/react';

export const songListStyle = css`
  margin: 20px;
  padding: 20px;
  background-color: #fff;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
`;

export const songItemStyle = css`
  margin-bottom: 10px;
  padding: 10px;
  border-bottom: 1px solid #ddd;
`;

export const songTitleStyle = css`
  font-size: 1.2em;
  font-weight: bold;
`;

export const songDetailStyle = css`
  color: #555;
`;
